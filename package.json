{
  "name": "petprojects",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/*.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build src/*.html --public-url /GO_IT/",
    "push": "git add . && git commit -m 'update' && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "os-browserify": "^0.3.0",
    "parcel": "^2.7.0",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "dependencies": {
    "@swc/helpers": "^0.4.11"
  }
}
